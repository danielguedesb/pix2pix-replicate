build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - "diffusers==0.27.0"
    - "torch==2.1.1"
    - "ftfy==6.2.0"
    - "scipy==1.12.0"
    - "transformers==4.38.2"
    - "accelerate==0.28.0"
  run:
    - pip install git+https://github.com/huggingface/diffusers.git

predict: "predict.py:Predictor"
